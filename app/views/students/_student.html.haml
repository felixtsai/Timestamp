%div
  %h3
    = full_name(student)
  %table{ :cellpadding => "3", :cellspacing => "2" }

    %tr
      %th Status
      %th Subject
      %th Description
      - student.student_assignments.each do |s_a|
        %tr
          %td
            -if s_a.completion_time
              %span.label.label-success
                = link_to "Complete", student_assignment_path(s_a, :completion_time => "NULL"), method: :put, :remote => true
            -else
              %span.label.label-important
                = link_to "Incomplete", student_assignment_path(s_a, :completion_time => Time.now), method: :put, :remote => true
          %td
            = s_a.assignment.subject.name
          %td
            = s_a.assignment.description




    -# %tr
    -#   %td= student.first_name + " " + student.last_name
    -#   %td
    -#     - if !student.find_session_attendance(@session.id)
    -#       %span.label
    -#         = link_to "Sign in", attendances_path(attendance: {session_id: @session.id, student_id: student.id}), method: :post, remote: :true, student_id: student.id
    -#       - else
    -#         = render partial: 'sign_in', locals: {student: student, session: @session}
    -#   %td
    -#     %span.label
    -#       = link_to "Show Assignments", student_path(student), method: :get, :remote => true